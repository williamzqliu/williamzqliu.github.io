node_modules/
dist/
.astro/
.env
.env.*
!.env.example
.DS_Store
Thumbs.db
CLAUDE.md
# Two of these travel with the repo, because they describe the project rather
# than the machine: `launch.json` is how the preview server is started and
# `settings.json` turns off the co-author trailer. `settings.local.json` is a
# per-machine permission allowlist and stays local.
.claude/*
!.claude/launch.json
!.claude/settings.json

# media-src/ is the raw archive: whatever came out of the camera, the screen
# recorder or the export dialog, named after the file it produces in
# public/media/. The site is built from public/, so the masters never need to
# travel with the repo — and they are tens of megabytes each. Local only, in
# every format; the published derivatives are what is versioned.
media-src/
