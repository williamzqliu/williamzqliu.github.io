/**
 * Cover media switch.
 *
 * The tone plates were standing in for loops and stills that do not exist
 * yet, and a page full of boxes labelled "cover loop" reads as unfinished
 * rather than as a placeholder. While this is false nothing renders a plate:
 * project rows and case study heads are text-only.
 *
 * Flip to true once the files each project declares in `cover.wide` are in
 * public/media/. Nothing else needs to change.
 */
export const COVERS_ENABLED = false;
